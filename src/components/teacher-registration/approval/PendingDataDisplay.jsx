import React from 'react';
// This component is no longer needed for teacher registration
// Teachers will see their submission status in the teacher dashboard instead

export default function PendingDataDisplay() {
    // Component removed - teachers only see pending items inline during registration
    // Approved/rejected items are viewed in teacher dashboard
    return null;
}